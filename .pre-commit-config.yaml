repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.2
    hooks:
      # v0.11.x uses id `ruff`; newer releases rename it to `ruff-check`.
      - id: ruff
        args: [--fix]
      - id: ruff-format
